description ''

dependencies {
    api gradleApi()
    compileOnly 'com.android.tools.build:gradle:3.0.0-alpha4'
}

jar {
    manifest {
        attributes(
                'Implementation-Title': 'placeholders-plugin',
                'Implementation-Version': VERSION
        )
    }
}